int		main(void)
{
	while ((ret = read(0, &buf[++i], 1)))
	{
		if ("<") --> enregister nom fichier dans var fichier : open(fichier, O_RDONLY) ; read/write fichier sur 0 avec fonction redirection;
	}
}
